<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle"
    android:color="@color/btn_disable">
    <corners
        android:radius="100dp" />
    <stroke
        android:width="0.5dp"
        android:color="@color/bg_main2" />
    <solid
        android:color="@color/bg_white" />
</shape>